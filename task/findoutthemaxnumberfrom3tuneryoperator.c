// find max number in 3 turnery operator
#include<stdio.h>
int main()
{
    int a,b,c;
    int ch;

    printf("\na:");
    scanf("%d",&a);

    printf("\nb:");
    scanf("%d",&b);

    printf("\nc:");
    scanf("%d",&c);

      (a > b) ? ((a > c) ? printf("\n A is max%d")) : printf("\n C is max:%d") ( (b > c)) ? printf("\n B is max:%d") : printf("\n C is max: %d"); 
         
      }